# slice_get :: @subscript (s: slice_t(!u), index: sint) -> &u {
#     assert(index >= 0 and index < s.count);
#     return offsetptr(s.data, index);
# };



main :: () -> void {
    arr := [!]sint.{1, 2, 3, 4};
    (@load "io.or").println(.{arr[2]});
};