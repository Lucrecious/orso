@subscript slice_get :: !(s: &slice_t(!u), index: sint) -> &u {
    assert(index >= 0 and index < s.count);
    return offsetptr(s.data, index);
};



main :: () -> void {
    arr := [!]sint.{1, 2, 3, 4};
    s := slice(&arr);

    (@load "io.or").println(.{s[2]});
};