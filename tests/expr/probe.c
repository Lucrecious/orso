#define INTRINSICS_IMPLEMENTATION
#include "intrinsics.h"

i32 expr(void) {
i32 result_; {
    i32 sum_ = i;

    i32 a_ = i;

    {
      if ((a_ == i)) {
        (sum_ = (sum_ + i));
      }
    };

    result_ = sum_;
;
  };

  return result_;
}
