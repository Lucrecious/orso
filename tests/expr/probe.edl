
{
    foo :: (n: int) -> int {
        if n > 0 then return tar(-1);
        return n;
    };

    tar :: (n: int) -> int {
        return bar(n-1);
    };

    bar :: (n: int) -> int {
        if n > 0 then return foo(-1);
        return n;
    };

    @run foo(10);
}

// {
//     @run foo(10);

//     bar :: (n: int) -> int {
//         if n > 0 then return foo(n - 1);
//         return n;
//     };

//     foo :: (n: int) -> int {
//         if n > 0 then return bar(n - 1);
//         return n;
//     };
// }