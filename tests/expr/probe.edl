
{
    mutate :: (n: &int) -> void {
        *n = 42;
        return;
    };

    x := 0;
    y := 0;

    xp := &x;
    xpp := &xp;

    **xpp = 42;

    x;

    *xpp = &y;

    **xpp = 42;

    y == x;
}
