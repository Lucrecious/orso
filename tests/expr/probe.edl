{
    add :: (a: int, b: int) -> int {
        return a + b;
    };

    sum := @run add(2, 3);

    sum;
}

{
    @run foo(10);

    bar :: (n: int) -> int {
        if n > 0 then return foo(n - 1);
        return n;
    };

    foo :: (n: int) -> int {
        if n > 0 then return bar(n - 1);
        return n;
    };
}