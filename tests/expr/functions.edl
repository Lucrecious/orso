{
    fib :: (n: i64) -> i64 {
        a := 0;
        b := 1;

        return while n > 0 {
            t := a;
            a = b;
            b = b + t;
            n = n - 1;
        } else {
            b;
        };
    };

    result := fib(4);

    result;
}