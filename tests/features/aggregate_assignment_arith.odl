main :: () -> void {
    {
        a := [4]int.{0, 1, 2, 3};
        b := [4]int.{0, 1, 2, 3};

        for i := 0; i < len(a); ++i {
            printint(a[i]);
            println();
        };
        
        println();

        a += b;

        for i := 0; i < len(a); ++i {
            printint(a[i]);
            println();
        };
        
        println();
    };
};