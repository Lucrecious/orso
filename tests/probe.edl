{
    // add :: (a: [2]!u, b: [2]u) -> [2]u {
    //     return .{a[0]+b[0], a[1] + b[1]};
    // };

    // a := [2]f32.{1, 2};
    // b := [2]f32.{10, 10};

    // c := add(a, b);

    // c[1];

    ;
    for
        i := 0;
        i < 10;
        ++i
    {
        printint(i);
        println();
        continue;
    };
    
}