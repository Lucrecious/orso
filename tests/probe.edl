{
    add :: (a: [!u]!v, b: [u]v) -> [u]v {
        c: [u]v;
        for i := 0sz; i < u; ++i {
            c[i] = a[i] + b[i];
        };

        return c;
    };

    vec2f :: [2]f32;
    vec4s :: [4]s32;

    c := add(vec4s.{1, 2, 3, 4}, .{5, 6, 7, 8});

    len(c);
}