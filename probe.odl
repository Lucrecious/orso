main :: () -> void {
    {
        sum := 0;
        s := 0;
        s = 1;
        sum = for i := 0; i < 4; ++i do
            if i == 0 then
                continue
            else s *= i
            then s;
        printint(if sum == 6 then 1 else 0);
        println();
    };

};